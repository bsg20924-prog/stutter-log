# 말막힘 일지 (stutter-log)

한국어 말더듬 기록·연습 PWA. 사용자 본인 1인용.
React 18 + TypeScript + Vite + Tailwind v4 + Firebase(Firestore·Hosting) + Gemini(선택).

**주석과 UI 문구는 전부 한국어로 쓴다.** 주석은 "무엇을"이 아니라 **"왜"**를 적는다 —
이 저장소의 주석은 대부분 "이렇게 안 하면 무슨 일이 벌어졌는지"의 기록이다. 그 밀도를 유지할 것.

---

## 1. 제품 원칙 — 코드보다 이게 먼저다

이 앱의 가치는 구현이 아니라 아래 원칙에 있다. **원칙을 어긴 채 동작하는 코드는 실패한 코드다.**

### 1.1 유창성을 목표로 삼지 않는다

말을 덜 더듬게 만드는 것이 목표가 아니다. 관찰하고, 피하지 않게 되는 것이 목표다.
따라서 앱은 사용자에게 **칭찬·격려·진단·처방을 하지 않는다.**

- 금지: "이번 주 덜 더듬었네요", "좋아지고 있어요", "잘하고 있어요", "화이팅"
- 금지: "~때문에 막히는 것 같아요" (원인 추정), "다음 주엔 ~해보세요" (조언)
- 허용: 담담한 관찰 서술. "기록 11건. 그중 막힘 3건."

⚠️ **LLM 을 쓰는 모든 경로에서 이게 가장 자주 깨진다.** LLM 은 시키지 않으면 응원단장이 된다.
프롬프트 금지 조항만으로는 강제되지 않으므로 **출력 검사 + 폴백**을 반드시 함께 둔다.
(`weeklyNarrative.ts` 의 금지어 검사, `liveConversation.ts` 의 규칙 5 참고)

### 1.2 막힘 순간에는 앱이 끼어들지 않는다

1. **막히는 동안: 앱 조작도, 자기관찰도 요구하지 않는다.** 자기관찰은 막힘을 악화시킨다.
   말하는 도중에 뜨는 프롬프트·타이머·"지금 기분이 어때요?" 같은 것은 절대 만들지 않는다.
2. **막힌 직후(~30초): QuickLog 는 3탭 이내로 끝나야 하고, 거친 질문만 한다.**
   이 시점의 사용자는 아직 흐트러져 있다. 정밀함을 요구하는 폼은 버려지거나 쓰레기 데이터를 만든다.
   상세 입력(`isDetailed`, 신체/감정 상태, 메모)은 **의도적으로 분리된 나중 단계**로 민다.

새 입력 흐름을 만들거나 고칠 때는 **완료까지의 탭 수를 세라.** 직후 경로에서 3탭 초과면 반려.
대상 파일: `QuickLogInput.tsx`, `LogForm.tsx`.

### 1.3 연습 모드에는 위 3탭 제약이 적용되지 않는다

`SoundMapTest`, `DiagnosticTest`, 10초 연습, `LiveTalk` 은 **통제된 노출·둔감화 환경**이다.
여기서 긴장을 관찰하고 자기평가를 모으는 것은 위반이 아니라 **기능**이다.
다단계 평가를 두어도 된다. 1.2 는 **실제 막힘 순간과 QuickLog 경로에만** 적용된다.

### 1.4 몸 감각 질문에는 항상 "모르겠음"이 있어야 한다

막힘 중·직후의 신체 감각은 원래 불확실하다. "모르겠음"이 없으면 사용자는
찍거나(데이터 오염) 폼을 버린다. **"모르겠음"은 건너뛰기가 아니라 1급 선택지**이며,
그 빈도 자체가 인식 수준을 보여주는 진짜 데이터다.

적용처: `LogForm.tsx`, `DiagnosticTest.tsx`, `ArticulationMap` 구역 선택, 소리 지도 평가 단계.
(`UNKNOWN_ASSESSMENT` in `data/soundMap.ts`)

⚠️ 단, **아예 말하지 않은 항목을 '모르겠음'으로 저장하면 안 된다.**
그건 "말은 했는데 판단이 안 됐다"는 뜻이라, 건너뛴 항목을 섞으면 인식 공백 지표가 오염된다.
건너뛰기는 아무것도 기록하지 않는다.

---

## 2. 측정 규칙 — 어기면 데이터가 조용히 썩는다

### 2.1 검사 단어는 문장 맨 앞에 와야 한다

말막힘은 **발화를 시작하는 순간**에 일어난다. 단어가 문장 중간이나 끝에 있으면
그 앞의 말로 이미 발성이 시작된 뒤라 **그 단어 때문에 막혔는지 판별할 수 없다.**

- 맞음: `{word} 주세요`
- 틀림: `제 관심사는 {word}입니다`

Gemini 가 만든 문장도 **코드에서 반드시 검증해서 거른다.** 지시문만으로는 모델이 어긴다.
(`gemini.ts` 의 `startsWith` 검사)

### 2.2 측정 모드와 노출 모드를 섞지 않는다

자유 발화(`LiveTalk`)에서는 2.1 을 지킬 수 없다. 그래서 **살아있는 대화 결과는
소리 지도 채점에 넣지 않는다.** 섞으면 "어느 소리가 어려운지"가 오염된다.

사다리: 속삭임 → 목소리 → 녹음 → 상황(측정) → **살아있는 대화(노출, 점수 없음)**

### 2.3 집계는 `status` 기준으로 한다

`outcome` 은 상세 기록에만 있고 간편 기록(3탭 경로)에는 비어 있다.
`outcome` 으로 묶으면 **실제로 가장 많이 쌓이는 기록이 통째로 빠진다.**
`status` 는 `useLogStore.migrate` 가 모든 기록에 항상 채워 준다.

### 2.4 표본이 적으면 비율을 말하지 않는다

기록 2~3건짜리 구간의 회피율은 한 건에 33%p 씩 튄다. 추세로 읽으면 거짓말이 된다.
`MIN_COMPARE_SAMPLE`(=5) 미만이면 비율 비교를 보여주지 않는다. (`trends.ts`)

빈 구간을 건너뛰지도 않는다 — x축이 압축돼 "꾸준히 기록한 것처럼" 보인다.

---

## 3. 아키텍처 규칙

### 3.1 Gemini 는 '덧붙이는 층'이다

**없어도 앱이 완전히 동작해야 한다.** 오프라인 PWA 이고, 검사 도중 네트워크가 끊겼다고
사다리를 못 올라가면 안 된다.

- 모든 Gemini 경로는 실패 시 **null 을 돌려준다. 예외를 던지지 않는다.**
- 호출부는 반드시 **자체 폴백**을 갖는다 (템플릿 문장, 고정 사다리, 브라우저 음성)
- 새 fetch 를 직접 쓰지 말고 `gemini.ts` 의 `callGemini()` / `parseGeminiJson()` 을 쓴다

### 3.2 API 키

백엔드가 없으므로 **`VITE_` 환경변수는 빌드 시 번들에 그대로 박힌다 = 공개다.**

- **Firebase 키**: 공개돼도 된다. 보안은 `firestore.rules` 의 `isOwner()` 가 담당한다.
- **Gemini 키**: 과금 자격증명이라 공개되면 요금이 청구된다.
  → 번들에 넣지 않는다. 사용자가 앱에서 입력하고 `localStorage` 에 두며,
    기기 간 동기화는 `isOwner()` 로 잠긴 Firestore 문서를 통한다.

### 3.3 비용 — TTS 오디오가 거의 전부다

| | 단가 |
|---|---|
| `gemini-3.6-flash` | $1.50 / $7.50 per 1M (입력/출력) |
| `gemini-3.1-flash-tts` | $1.00 텍스트입력 / **$20.00 오디오출력** per 1M |

오디오는 **초당 25토큰**. 텍스트 생성 비용은 반올림하면 0이고 **비용의 90%가 TTS** 다.

### 3.3.1 TTS 한도 — 돈보다 이게 먼저 막힌다 (2026-07-30 실측)

**유료 Tier 1 인데도 TTS 는 모델당 분당 10회 · 하루 100회다.** 결제로 풀리지 않는다.

| | |
|---|---|
| RPM (분당 요청) | **10** |
| RPD (일일 요청) | **100** |

- **이 엔드포인트에는 GA TTS 모델이 없다.** 전부 preview 이고, 그래서 한도가 낮다.
  GA 인 `gemini-2.5-flash-tts` 는 Cloud Text-to-Speech / Vertex 쪽에만 있는데
  서비스 계정 인증이 필요해 백엔드 없는 이 앱에서는 쓸 수 없다.
- **모델마다 할당량이 따로다.** 그래서 마르면 다음 모델로 넘어간다(`geminiTts.ts` 의 `MODELS`):
  `gemini-3.1-flash-tts-preview` → `gemini-2.5-flash-preview-tts` → `gemini-2.5-pro-preview-tts`.
  실질 하루 300회. pro 는 비싸므로 마지막이다.
- ⚠️ **타임아웃으로 끊은 요청(499)도 하루 100회에서 차감된다.** 성급하게 끊으면
  할당량만 버리고 아무것도 못 얻는다. 재시도도 마찬가지다 — 재시도 한 번이
  받을 수 있는 개수를 그대로 줄인다.
- 그래서 미리받기는 **동시 1개 · 6.5초 간격 · 재시도 1회**다. 이걸 늘리면 반드시 역효과가 난다
  (동시 4개로 올렸다가 429 → 60초 백오프 → 동시 재개 → 또 429 로 영영 멈춘 적이 있다).

### 3.3.2 그래서 실시간 생성에 기대지 않는다

TTS 생성은 5~15초, 종종 더 걸리고 과부하 시 500 을 뱉는다. 대화 도중에 만들면
그 불안정이 그대로 사용자에게 간다. **고정 대사는 미리 받아 IndexedDB 에 영구 보관하고
(`ttsStore.ts`, `ttsPrepare.ts`), 대화 중에는 AI 가 고르기만 한다**(`liveConversation.ts`).
받아 둔 뒤에는 대화가 네트워크를 타지 않는다.

### 3.4 iOS Safari — 이게 오디오 코드 구조를 거의 다 결정한다

1. **소리는 사용자 터치 핸들러 안에서 동기적으로 시작해야 한다.**
   `primeAudio()` 와 `speakPrompt()` 사이에 `await` 를 끼우면 제스처 컨텍스트가 끊겨
   **소리가 조용히 사라진다.** 그래서 `speakPrompt` 는 async 가 아니라 콜백을 받는다.
2. **제스처 밖에서 소리를 내야 하면** (예: Gemini 응답을 await 한 뒤)
   `speech.ts` 의 `unlockAudioElement()` 로 미리 잠금을 푼 공용 `<audio>` 엘리먼트를 쓴다.
3. **`utterance.onend` 가 아예 발생하지 않는 WebKit 버그가 있다.**
   워치독 타이머가 없으면 화면이 영원히 멈춘다 — 반드시 둔다.
4. `getVoices()` 는 처음에 빈 배열을 주고 `voiceschanged` 이후 채워진다. 미리 데워 둔다.
5. `MediaRecorder` 는 `audio/webm` 을 지원하지 않는다 → `audio/mp4` 폴백.
6. 마이크 권한 요청도 제스처 안에서 해야 한다 (`recorder.arm()`).
7. **음성인식(STT)은 침묵이 이어지면 스스로 종료한다.**
   막힘은 *말을 시작하기 전 침묵*으로 나타나므로 기본 동작대로 두면
   **하필 막힌 발화만 골라서 못 받는다.** 응답 창이 열려 있는 동안 `onend` 에서 자동 재시작한다.

### 3.5 STT 실패를 사용자의 실패로 보이게 하지 않는다

기계가 내 말을 못 알아듣는 경험은 이 앱이 줄이려는 바로 그 상처다.

- 인식 실패는 오류 화면이 아니라 **극중 대사**("네?")로 흡수한다
- 단, **연속 두 번 되묻지 않는다.** 두 번째부터는 알아들은 셈 치고 넘어간다
- 말풍선에 "안 들렸어요" 같은 문구를 쓰지 않는다 — 판정처럼 보이는 순간 연습이 아니게 된다

---

## 4. 명령어

```bash
npm run dev          # 개발 서버
npx tsc --noEmit     # 타입 검사
npm run build        # tsc + vite build
```

### 배포는 수동이다 — **push 해도 배포되지 않는다.** CI 가 없다.

```bash
npm run build && firebase deploy --only hosting,firestore:rules
```

- ⚠️ **`firestore:rules` 를 반드시 포함한다.** 규칙은 번들과 따로 나가므로,
  hosting 만 배포하면 UI 의 쓰기가 낡은 규칙에 막혀 permission-denied 가 난다. 실제로 겪었다.
- ⚠️ **`.env.local` 은 gitignored 이고 이 기계에만 있다.** 없어도 빌드는 **성공**하지만
  Firebase 설정이 전부 `undefined` 로 인라인되어 런타임에 깨진다 — 조용한 고장이다.
  값은 커밋 `6765b8b` 이전 `src/firebase.ts` 히스토리에서 복구할 수 있다.
- 캐시 헤더는 `firebase.json` 에 이미 잡혀 있다. 루트(`/`)가 캐시되어 배포가 1시간 안 보인 적이 있으니
  헤더 블록을 함부로 건드리지 말 것.

---

## 5. 코드 관용구

- **Tailwind 카드**: `bg-white rounded-2xl shadow-[0_8px_30px_rgb(0,0,0,0.04)] p-4`
- **제목**: `text-sm font-semibold text-gray-700` / **본문**: `text-xs text-gray-600 leading-relaxed`
- **보조 문구**: `text-[11px] text-gray-400` / **강조색**: teal
- 한글 줄바꿈은 `break-keep`
- 아이콘은 `lucide-react`
- 커밋 메시지: `feat(scope): 한국어 설명` (conventional commits + 한국어 본문)

---

## 6. 에이전트에게

- **설계·지시에 없던 판단을 했으면 반드시 보고하라.** 이 저장소에서 중요한 결정은
  대부분 코드를 쓰는 도중에 발견된다. 조용히 정하고 넘어가면 그게 원칙 위반이 된다.
- **지시 없이 커밋·푸시·배포하지 마라.**
- **`.env.local` 을 건드리거나 출력하지 마라.**
- 확신이 없으면 "확인 못 함"으로 보고하라. 틀린 확인은 확인을 안 한 것보다 나쁘다.
